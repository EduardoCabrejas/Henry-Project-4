export interface RegForm {
        name: string;
        email: string;
        password: string;
        address: string;
        phone: string;
}

export interface LogForm {
    email: string;
    password: string;
}
