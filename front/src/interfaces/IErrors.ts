export interface RegErrors {
    name: string;
    email: string;
    password: string;
    address: string;
    phone: string;
}

export interface LogErrors {
    email: string;
    password: string;
}